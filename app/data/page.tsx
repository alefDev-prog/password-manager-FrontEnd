import {Data} from "./components/data";
import getInfoTest from "@/lib/getUserInfo";


export default async function DataPage() {
   
   
   /* const id = sessionStorage.getItem('userId');

    if(typeof id == "string") {
        const userPromise = getInfoTest(id);

    }
    */
    return (
        <Data></Data>
    )
}